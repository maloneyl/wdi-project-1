- if can? :update, @music

  .row
    .col-md-8.col-md-offset-1
      %h2 Edit Track

      = render 'form'

      %p

      = link_to 'Remove Track', @music, method: :delete, confirm: "Are you sure you'd like to remove #{@music.title}? There's no undo!"
